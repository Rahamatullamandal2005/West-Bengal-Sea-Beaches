// Assuming the element has the ID "myElement" (replace with actual element)
if (document.getElementById("myElement")) {
  const elementTop = document.getElementById("myElement").offsetTop;
  // Use elementTop here
} else {
  // Handle the case where the element is not found
  console.log("Element with ID 'myElement' not found");
}